# Local: copy to .env (this repo ships .env for convenience; adjust if your API port differs).
# Backend: run igltf-editor-backend/run-back.bat or uvicorn on the same host/port.
VITE_API_BASE_URL=http://127.0.0.1:8000
